NOTAS IMPORTANES:
ForEach(Comando)
MAX(TRANSFORMER)//Te   devuelve el poder
SUM(TRANSFORMER)
MAP(TRANSFORMER)//Te devuelve el artefacto(otro Conjunto)// Siempre devuleve una lista
Filer(predicate)// Devuelve lo que se filtra (listas o Conjuntos)
All(predicate)//Si cumplen todos
Any(predicate)//Si solo hay 1 que cumple
find(predicate)//El primer objeto que cumpla ese predicado
